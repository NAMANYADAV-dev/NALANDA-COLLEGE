/** PostCSS pipeline — Tailwind compiles utility classes, Autoprefixer adds vendor prefixes. */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
